# Match IT landing page

A one-page bespoke site for the Match IT founder team, prepared ahead of eMerge Americas 2026 (Booth SS36). The page opens with the Eduba read on where AI actually fits in a matching-platform stack (the 60 / 30 / 10 frame), maps the shape to the Feeld sprint as a prior pattern, points to the Interpretable Context Methodology paper, and closes with a 30-minute booking link for Matt Creamer.

Brand tokens are the AI / deep-tech category fallback (navy primary, coral accent, Inter + JetBrains Mono). Confidence on the tokens is low because no confirmed Match IT website was available at research time. If Matt captures a real brand reference at the booth, swap tokens in `styles.css` `:root` and the hero wordmark in `index.html`.

Intended URL path: /for/match-it
